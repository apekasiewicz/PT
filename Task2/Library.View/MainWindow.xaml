<Window x:Class="Library.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="ReadersList">
            <local:ReaderListView/>
        </DataTemplate>
        <DataTemplate x:Key="EventsList">
            <local:EventListView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions/>
        <ContentControl Content="{Binding}" ContentTemplate="{StaticResource ReadersList}" Grid.Row="0"/>
        <!--" ContentControl Content="{Binding}" ContentTemplate="{StaticResource EventsList}" Grid.Row="1"/-->
    </Grid>
</Window>
